---
title: Welcome to Middleman
---

<%= partial "shared/nav" %>
<%= partial "shared/header" %>

<main class="border-t border-slate-200 lg:relative lg:mb-16 lg:ml-[500px] lg:border-t-0 xl:ml-120">
  <h2 class="text-lg text-gray-500 border-b p-8 lg:px-16 font-bold">Episodes</h2>

  <% blog.articles.each do |podcast| %>
    <article class="<%= "border-b" unless podcast == page_articles.last %> border-dashed p-8 lg:px-16 lg:py-12">
      <time datetime="<%= podcast.date %>" class="order-first font-mono text-sm leading-7 text-slate-500"><%= podcast.date.strftime('%b %e, %Y') %></time>
      <h3 class="mb-2 text-xl font-black leading-tight text-gray-900 transition duration-300 group-hover:text-secondary-500 ease-in-out"><%= "#{podcast.data.episode}: #{podcast.title}" %></h3>
      <p class="text-gray-700 font-base"><%= podcast.data["teaser"] %></p>
    </article>
  <% end %>
</main>

<%= partial "shared/player" %>
